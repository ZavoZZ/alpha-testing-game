# .env.production - Production Environment Variables Template
# ============================================================
# IMPORTANT: Copy this file to .env.production and fill in the actual values
# DO NOT commit this file to Git with real secrets!
# 
# Usage:
#   cp .env.production .env.production.local
#   # Edit .env.production.local with actual values
#   docker compose -f docker-compose.production.yml --env-file .env.production.local up -d

# =====================================================================
# DATABASE
# =====================================================================
DB_NAME=game_db

# =====================================================================
# JWT SECRETS - CRITICAL SECURITY
# =====================================================================
# Generate strong random strings for production!
# Use: openssl rand -base64 32
SECRET_ACCESS=CHANGE_THIS_TO_A_STRONG_RANDOM_STRING_MIN_32_CHARS
SECRET_REFRESH=CHANGE_THIS_TO_A_STRONG_RANDOM_STRING_MIN_32_CHARS

# =====================================================================
# GAME ACCESS PASSWORD
# =====================================================================
# Password required to access the game
GAME_PASSWORD=CHANGE_THIS_TO_A_SECURE_PASSWORD

# =====================================================================
# DOMAIN CONFIGURATION
# =====================================================================
# Your production domain (e.g., https://ovidiuguru.online)
# This is used for CORS and generating URLs
WEB_ORIGIN=https://ovidiuguru.online

# =====================================================================
# OPTIONAL: MongoDB Atlas (if using external database)
# =====================================================================
# Uncomment and set if using MongoDB Atlas instead of local MongoDB
# DB_URI=mongodb+srv://username:password@cluster.mongodb.net/game_db?retryWrites=true&w=majority

# =====================================================================
# OPTIONAL: Qdrant Vector Database
# =====================================================================
# Uncomment if using Qdrant for AI features
# QDRANT_URL=http://qdrant:6333
# QDRANT_COLLECTION=mern-game-codebase

# =====================================================================
# NOTES
# =====================================================================
# 1. Always change the default values above!
# 2. Keep this file secure - don't share secrets
# 3. For deployment, use --env-file flag or set environment variables
# 4. Check logs with: docker compose -f docker-compose.production.yml logs -f
